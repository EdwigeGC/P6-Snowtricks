{% extends 'base.html.twig' %}

{% block title %}Edit a video{% endblock %}

{% block body %}
<div class="container">
    <h1 class="h3 mb-3 fw-normal">Edit a video</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}

     <button class="btn btn-primary" type="submit">Save</button>

    {{ form_end(form) }}
{% endblock %}